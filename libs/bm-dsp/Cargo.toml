[package]
name = "bm-dsp"
description = "bit-music numeric audio primitives (resampling, mixing, normalization)"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
